{
  "name": "akari",
  "version": "0.1.0",
  "description": "AKARI (灯) — a gentle memorial app to remember loved ones' anniversaries and birthdays. Local-only, no data collection.",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "assets": "npx @capacitor/assets generate"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
